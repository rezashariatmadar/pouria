@# یدک‌پرو (YadakPro) — Handover & Master Context Document
> **Target Audience:** Incoming AI Agent / Engineering Collaborator  
> **Last Updated:** September 6, 2026  
> **Status:** Production-ready interactive prototype deployed on Vercel; Ready for client review & full-stack development.

---

## ۱. خلاصه‌ی پروژه و کارفرما (Executive Overview)
* **کارفرما (Client):** پوریا (Pouria) — فعال بازار لوازم یدکی خودرو در تهران (چراغ‌برق / بازار اکباتان).
* **حوزه فعالیت (Domain):** پلتفرم تخصصی تجارت الکترونیک، انبارداری و استعلام قیمت قطعات یدکی خودروهای **ایران‌خودرو (IKCO)** و **سایپا (SAIPA)**.
* **برند پروژه:** یدک‌پرو (YadakPro).
* **کاربر نهایی / توسعه‌دهنده سیستم:** Dante (سیستم‌عامل: **Arch Linux / Omarchy**).
* **آدرس استقرار زنده (Live Production URL):**
  👉 **[https://pouria-kappa.vercel.app](https://pouria-kappa.vercel.app)**
* **دستور دیپلوی به ورسل:**
  ```bash
  npx vercel --prod --yes
  ```

---

## ۲. دغدغه‌های اصلی بیزینس پوریا (Core Business Moats & Requirements)
هر ایجنتی که توسعه را ادامه می‌دهد **باید** این ۳ اصل بازار لوازم یدکی ایران را بداند:

1. **جلوگیری از مرجوعی کالا با استعلام شاسی (VIN Verification):**
   * بزرگترین معضل فروشگاه‌های آنلاین لوازم یدکی، خرید اشتباه قطعه توسط مشتری به دلیل تفاوت تیپ‌هاست (مثلاً تفاوت ای‌سی‌یو بوش با غرب‌استیل در ۲۰۶، یا تفاوت سیم‌کشی کوییک در سال‌های مختلف).
   * **راهکار:** شبیه‌سازی سیستم استعلام ۱۷ رقمی VIN (`NAAP...` برای ایران‌خودرو، `NAS...` برای سایپا) در صفحه اصلی و صفحه محصول و بخش تایید انباردار در پنل ادمین.

2. **تغییرات فوق‌سریع قیمت به دلیل نوسانات بازار ارز (Rapid Price & Inventory Grid):**
   * فروشندگان قطعه نمی‌توانند برای تغییر قیمت تک‌تک وارد صفحه ادیت محصول شوند.
   * **راهکار:** یک جدول سوپراسپید با ناوبری کلیدهای کیبورد (Arrow Keys + Enter)، سوییچ فوری «تماس بگیرید»، و امکان اعمال درصد افزایش/کاهش گروهی.

3. **مدیریت تنوع قطعات جفتی / متقارن (Positional Variants):**
   * قطعاتی مثل چراغ، آینه، کمک‌فنر، طبق و لنت نیاز به تفکیک سمت راست (شاگرد)، سمت چپ (راننده) و جفت دارند تا از سردرگمی خرید جلوگیری شود.

---

## ۳. معماری فایل‌ها و وضعیت فعلی پروژه (File Architecture)

```
/home/dante/Work/pouria/
├── DEVELOPMENT_SPECS.md        # مستند معماری و مشخصات فنی نسخه نهایی پروداکشن (Next.js/PostgreSQL)
├── PROTOTYPE_SPECS.md          # مشخصات و فیچرهای تفصیلی پروتوتایپ ۶ صفحه‌ای
├── car_parts_discovery_questions.md # سوالات و سناریوهای مصاحبه با پوریا
├── HANDOVER.md                 # همین فایل (سند تحویل برای ایجنت بعدی)
├── vercel.json                 # روت‌های Vercel (هدایت / به /prototype/index.html)
├── package.json                # پکیج‌های توسعه و ابزار تست Playwright
├── node_modules/               # ماژول‌های محلی
│
└── prototype/                  # پورتال اصلی پروتوتایپ تعاملی
    ├── index.html              # صفحه اصلی انتخاب پروتوتایپ‌ها و سناریوهای مصاحبه با پوریا
    │
    ├── store/                  # فروشگاه خریدار (Storefront)
    │   ├── index.html          # صفحه اصلی فروشگاه:
    │   │                       #  - نقشه تعاملی شاسی (SVG Technical Blueprint) با ۶ رادار نوری تپنده
    │   │                       #  - بازرسی زنده قطعات (Live Inspector) و سوییچ ۴ پلتفرم خودرو
    │   │                       #  - شبیه‌ساز کارت هوشمند خودرو و دیکودر VIN با پریست‌های تست
    │   │                       #  - ۸ کارت محصول با افکت شیب سه‌بعدی (3D Tilt) بر اساس حرکت موس
    │   │                       #  - پاپ‌آپ مشاهده سریع (Quick View Modal)
    │   │                       #  - نوار اعلام خریدهای زنده شهرهای ایران (Social Proof Ticker)
    │   │
    │   ├── catalog.html        # کاتالوگ ۱۲ محصولی:
    │   │                       #  - فیلتر برند، وضعیت، دسته‌بندی و خودرو
    │   │                       #  - دراور شناور و مدال مقایسه دو قطعه (Side-by-Side Comparison)
    │   │
    │   └── product-detail.html # صفحه محصول (چراغ کوییک کروز):
    │                           #  - گالری ۴ زاویه (روبرو، پشت و موتور تنظیم، سوکت فابریک، کارتن پلمپ)
    │                           #  - ذره‌بین زوم مختصاتی با حرکت موس (Zoom Lens)
    │                           #  - بخش باندل هوشمند (خرید ست کامل با محاسبه خودکار تخفیف)
    │                           #  - اسکنر زنده شماره شاسی با نوار پیشرفت
    │                           #  - نوار چسبان خرید سریع در نمای موبایل (Sticky Mobile Bar)
    │
    └── admin/                  # پنل ادمین پوریا (Management Dashboard)
        ├── index.html          # جدول فوق‌سریع قیمت و موجودی (ناوبری با کیبورد و دکمه‌های فوری)
        ├── product-add.html    # فرم تعریف قطعه جدید با تفکیک تنوع چپ/راست و تگ‌های خودرو
        └── orders.html         # کارتابل سفارشات، استعلام شاسی و پیشگیری از ارسال اشتباه
```

---

## ۴. جزییات فنی و فرانت‌اند (Frontend & Tech Stack)
1. **استک پروتوتایپ:**
   * **HTML5 + Vanilla JavaScript + Tailwind CSS (CDN) + Vazirmatn Font.**
   * بدون هیچ فریم‌ورک سنگین اضافه تا روی اینترنت موبایل ایران با سرعت زیر ۱ ثانیه لود شود.
   * کاملاً راست‌چین (RTL) و ریسپانسیو (موبایل و دسکتاپ).
2. **کپی‌رایت:** طبق تایید کاربر، برای تصاویر تستی پروتوتایپ هیچ محدودیتی وجود ندارد و از عکس‌های باکیفیت قطعات خودرو (Unsplash و منابع صنعتی) استفاده شده است.
3. **محیط کاربر (User OS):**
   * سیستم‌عامل کاربر **Arch Linux** است.
   * از اجرای دستورات `apt-get` یا پرچم‌های ناسازگار خودداری شود. ابزار بسته‌بندی محلی `pacman` یا `npm` است.
   * نود جی‌اس: `v26.7.0`.

---

## ۵. نحوه تست و بازرسی محلی (Testing & DevTools)
درایور اختصاصی Chromium در مسیر `~/.cache/ms-playwright/chromium-1243/chrome-linux64/chrome` نصب است.  
اسکریپت بازرسی تست خودکار در مسیر اسکرچ قرار دارد و می‌توانید آن را مستقیماً با دستور زیر اجرا کنید:
```bash
NODE_PATH=/home/dante/Work/pouria/node_modules node /home/dante/.gemini/antigravity-ide/brain/ff9ebed7-4280-4cb5-b224-65fb8b039c54/scratch/inspect_prototype.js
```
این اسکریپت سلامت ۱۰۰٪، نبود خطای شبکه (0 Network Failures)، کارکرد رادارهای شاسی، و لودینگ بدون باگ را تایید می‌کند.

---

## ۶. نقشه راه و گام‌های بعدی برای ایجنت جدید (Next Agent Action Items)

### فاز اول: جلسه با کارفرما و تثبیت نیازمندی‌ها (Client Validation)
1. لینک استقرار را با پوریا بررسی کنید: `https://pouria-kappa.vercel.app`
2. سه سوال کلیدی را ارزیابی کنید:
   * آیا جدول ادمین (`admin/index.html`) سرعت عمل روزانه پوریا را پوشش می‌دهد؟
   * آیا متغیرهای چپ و راست در کاتالوگ و صفحه محصول شفاف هستند؟
   * آیا تاییدیه شاسی (VIN) حس اعتماد را به خریدار منتقل می‌کند؟

### فاز دوم: تبدیل به فول‌استک (Full-Stack Engineering)
طبق مستند `DEVELOPMENT_SPECS.md`:
1. **فریم‌ورک هدف:** Next.js (App Router) + TypeScript + Tailwind CSS.
2. **پایگاه داده:** PostgreSQL (با Prisma یا Drizzle ORM).
3. **مدل‌های داده اصلی:**
   * `Vehicle` (سازنده، مدل، تیپ، سال، حجم موتور).
   * `Part` (عنوان، پارت‌نامبر، برند، OEM، سازگاری، دسته‌بندی).
   * `PartVariant` (سمت: راست/چپ/جفت، قیمت روز، وضعیت تماس بگیرید، موجودی).
   * `Order` (اقلام، شماره شاسی ۱۷ رقمی خودرو، آدرس، وضعیت تایید کارشناس).
4. **کش و بهینه‌سازی:** Redis برای کش استعلامات مکرر قیمت و کش کاتالوگ خودروها.

### فاز سوم: سرویس‌های بومی ایرانی (Local Integrations)
1. **درگاه پرداخت:** اتصال به زرین‌پال، سداد یا به‌پرداخت ملت.
2. **سرویس پیامک (SMS OTP & Pattern):** کاوه‌نگار یا فراز اس‌ام‌اس برای ارسال وضعیت سفارش و تایید شاسی.
3. **موتورهای جستجوی کالا:** خروجی API استاندارد ترب (Torob) و ایمالز (Emalls) برای به‌روزرسانی لحظه‌ای قیمت‌ها.
